{
  "rootDirectory": "apps/web",
  "buildCommand": "pnpm install && pnpm run prebuild && pnpm run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "TSC_COMPILE_ON_ERROR": "true"
  }
}
